# Copy to .env.local for local dev (never commit .env.local).

# Origin бекенда SkladBase, без кінцевого "/"
VITE_API_BASE=http://localhost:8000

# Фолбек на dev (поза Telegram): валідний initData з scripts/dev_initdata.py
# (бекенд), напр.: python scripts/dev_initdata.py
VITE_DEV_INIT_DATA=
